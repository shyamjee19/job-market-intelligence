# Core project dependencies for the collector -> ETL -> Postgres pipeline.
#
# The Kafka/Spark/Snowflake/Airflow stack under docker/, spark/, kafka/,
# and snowflake/ is a future extension, not part of the active pipeline -
# its dependencies live separately (docker/airflow/requirements.txt) so
# they don't conflict with this environment.

requests==2.32.3
python-dotenv==1.0.1
psycopg2-binary==2.9.9

# --- API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# --- Auth (auth/) ---
bcrypt==4.2.0
PyJWT==2.9.0
email-validator==2.2.0

# --- Caching / background jobs - inactive until REDIS_URL is set
# (see database/cache.py, jobs/celery_app.py) ---
redis==5.1.1
celery==5.4.0

# --- AI module (ai/) ---
# openai's SDK also serves the Azure OpenAI provider (AzureOpenAI client).
# anthropic/pinecone are only imported by their respective provider/store -
# see ai/providers and ai/vectorstore for the "unconfigured -> inactive"
# pattern also used by collector/sources.
openai==1.54.0
anthropic==0.39.0
pinecone==5.4.2
numpy==2.1.3

pytest==8.3.3
httpx==0.27.2
